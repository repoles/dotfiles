# Bind the up/down keys to navigate history
# bindkey '^[[A' history-search-backward
# bindkey '^[[B' history-search-forward

# Bind the up/down keys to zsh-history-substring-search
bindkey '^[[A' history-substring-search-up
bindkey '^[[B' history-substring-search-down

# Skip forward/back a word with Opt + arrow
# bindkey '[D' backward-word
# bindkey '[C' forward-word

# Move to the start/end of the line with the Home/End keys
# bindkey '^[[H' beginning-of-line
# bindkey '^[[F' end-of-line

# Make Shift+Tab go back in the list of autocomplete suggestions
bindkey '^[[Z' reverse-menu-complete
