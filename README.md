# Dotfiles

Configurações do zsh, git, vim, mysql, bundle, etc.

# Instalação

Estando no diretório `dotfiles`:

```sh
ln -s "$(pwd)" ~/.config
ln -s ~/.config/zsh/.zshenv ~/.zshenv
ln -s ~/.config/gemrc ~/.gemrc
```
